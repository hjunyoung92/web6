<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.dao.MsgMapper">
  <select id="getTime" resultType="string">
    select sysdate from dual
  </select>

	<insert id="insertMsg" parameterType="MsgVO">
	insert into TBL_TEAM_MSG (mno, sender, whom, MSG)
	values(seq_team_msg.nextval,#{sender},#{whom},#{MSG)
	</insert>
	<insert id="insertFile" parameterType="MsgFileVO">
	insert into tbl_team_msg_file(fno, mno, savename, originname)
	values (seq_team_msg_file.nextval, seq_team_msg.currval,#{savename},#{originname})
	</insert>
</mapper>